# Vercel-only dependency set. Deliberately not the root requirements.txt:
# the serverless bundle has a hard 250MB unzipped ceiling, and uvicorn (Vercel
# supplies its own server) plus pytest are pure weight here.
pandas>=2.2
openpyxl>=3.1
matplotlib>=3.8
pydantic>=2.7
pydantic-settings>=2.3
python-dotenv>=1.0
httpx>=0.27
tenacity>=8.3
fastapi>=0.111
python-multipart>=0.0.9
SQLAlchemy>=2.0
psycopg[binary]>=3.1
PyJWT>=2.8
redis>=5.0
