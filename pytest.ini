[pytest]
testpaths = tests
pythonpath = . tests
addopts = -q --strict-markers
markers =
    live: hits the real OpenRouter API (requires OPENROUTER_API_KEY; skipped unless RUN_LIVE_TESTS=1)
filterwarnings =
    ignore::DeprecationWarning
